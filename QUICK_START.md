# ⚡ Quick Start Guide

دليل سريع لبدء استخدام Markdown to HTML Converter

---

## 🚀 البدء السريع (3 دقائق)

```bash
# 1. Clone المشروع
git clone https://github.com/MrTurki2/markdown-to-html.git
cd markdown-to-html

# 2. تثبيت التبعيات
composer install

# 3. إعداد البيئة
cp .env.example .env
php artisan key:generate

# 4. إعداد قاعدة البيانات
touch database/database.sqlite
php artisan migrate

# 5. تشغيل
php artisan serve
```

✅ افتح: `http://localhost:8000`

---

## 📖 الاستخدام السريع

### الواجهة العامة (/)

```markdown
1. افتح http://localhost:8000
2. الصق Markdown في الحقل الأيسر
3. اضغط "تحويل الآن"
4. شاهد HTML الجميل على اليمين!
```

### المختبر (/lab)

```markdown
1. افتح http://localhost:8000/lab
2. اختر محرك (Marked.js أو CommonMark)
3. الصق نص للاختبار
4. اضغط "اختبار"
5. شاهد الإحصائيات (الوقت، الذاكرة، الحجم)
```

---

## 🔌 استخدام API

### تحويل Markdown

```bash
curl -X POST http://localhost:8000/convert \
  -H "Content-Type: application/json" \
  -d '{
    "markdown": "# مرحباً\n\nهذا **نص عريض**",
    "engine": "commonmark"
  }'
```

### اختبار محرك في المختبر

```bash
curl -X POST http://localhost:8000/lab/test-engine \
  -H "Content-Type: application/json" \
  -d '{
    "markdown": "# Test\n\nThis is a test.",
    "engine": "commonmark"
  }'
```

---

## 🎯 أمثلة Markdown

### مثال بسيط

```markdown
# عنوان رئيسي

هذا نص عادي مع **نص عريض** و *نص مائل*.

## عنوان فرعي

- قائمة نقطية
- عنصر آخر
  - عنصر فرعي

1. قائمة مرقمة
2. عنصر ثاني
```

### مثال متقدم

```markdown
# مشروع رائع 🚀

## المميزات

- سريع ⚡
- جميل 🎨
- مجاني 💰

## مثال كود

\`\`\`javascript
function hello(name) {
    return `مرحباً ${name}!`;
}
\`\`\`

## جدول

| الميزة | الحالة |
|--------|--------|
| Marked.js | ✅ |
| Rust | 🔜 |

> نصيحة: جرب نسخ هذا المثال!
```

---

## 🛠️ حل المشاكل السريع

### المشروع لا يعمل؟

```bash
# تأكد من التبعيات
composer install

# تأكد من المفتاح
php artisan key:generate

# تأكد من قاعدة البيانات
php artisan migrate:fresh
```

### خطأ في عرض الصفحة؟

```bash
# امسح الـ cache
php artisan cache:clear
php artisan view:clear
php artisan config:clear
```

### المنفذ 8000 مستخدم؟

```bash
# استخدم منفذ آخر
php artisan serve --port=8001
```

---

## 📚 الخطوات التالية

1. **جرب الواجهة العامة**: ابدأ بتحويل Markdown بسيط
2. **استكشف المختبر**: قارن بين المحركات المختلفة
3. **اقرأ الوثائق**: راجع [README.md](README.md) للتفاصيل
4. **ساهم في المشروع**: Fork + Pull Request

---

## 💡 نصائح سريعة

### للاستخدام اليومي
- استخدم **Marked.js** للتحويل السريع
- استخدم زر "مثال" لرؤية القدرات
- جرب "تحميل" للحصول على HTML كامل مع الأنماط

### للمطورين
- استخدم `/lab` لاختبار المحركات
- جرب `Benchmark` لقياس الأداء
- راجع API endpoints للتكامل

### للتجارب
- جرب نصوص كبيرة في المختبر
- قارن بين CommonMark و Marked.js
- اختبر النصوص العربية والـ RTL

---

## 🎓 تعلم المزيد

- [Markdown Guide](https://www.markdownguide.org)
- [GitHub Flavored Markdown](https://github.github.com/gfm/)
- [Marked.js Docs](https://marked.js.org)
- [CommonMark Spec](https://commonmark.org)

---

**🎉 الآن أنت جاهز! ابدأ التحويل!**

افتح `http://localhost:8000` وابدأ العمل! 🚀
